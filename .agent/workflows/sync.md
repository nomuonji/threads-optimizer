---
description: Sync posts for all accounts or a specific account.
---

1. If the user specified an account ID (e.g., "sync account 123"), run sync for that ID.
2. `npm run sync -- --account=[ACCOUNT_ID]`

3. If no account is specified, sync all accounts.
// turbo
4. `npm run sync`
